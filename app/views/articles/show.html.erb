

<h1>showing articles details</h1>

<!-- embedded ruby code if don't put = it evaluate that code 
                                     it doesn't say anything about displaying
                                    = this means evaluate and render to the screen -->
                                    
<p><%=@article.title%></p>         
<p><%=@article.description%></p>
<%= link_to 'Edit', edit_article_path(@article) , data: { confirm: "Are you sure?" }%>|<%= link_to 'Delete', article_path(@article), method: :delete , data: {confirm: "Are you sure?"}%>|<%= link_to 'articles listing ', articles_path%>